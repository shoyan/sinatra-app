<div class="container">
  <% @memos.each do |memo| %>
    <section>
      <header>
        <h2>
          <a href="/<%= memo.title %>"><%= memo.title %></a>
        </h2>

        <time datatime="<%= memo.created_at.to_s %>">
            <%= memo.created_at.to_s %>
        </time>
      </header>

      <div class="body">
        <%= memo.body %>
      </div>
    </section>
  <% end %>
</div>
