<div class="container">
  <div>
    <h1><%= @memo.title %></h1>
  </div>
  <div>
    <p><%= @memo.body %></p>
  </div>
  <div>
    <form action="/memos/<%= @memo.id %>" method="POST">
      <input type="hidden" name="_method" value="DELETE">
      <div class="form-group">
        <input type="submit" value="削除" class="btn btn-danger">
        <a href="/memos/<%= @memo.id %>/edit"><input type="button" value="編集" class="btn btn-primary"></a>
      </div>
    </form>
  </div>
</div>
